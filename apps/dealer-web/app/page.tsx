export default function Home() {
  return (
    <main className="p-8">
      <h1 className="text-xl font-medium">AutoServe — Dealer web app</h1>
      <p className="text-sm text-gray-500">Scaffold only — wire up /login and the dashboard next.</p>
    </main>
  );
}
